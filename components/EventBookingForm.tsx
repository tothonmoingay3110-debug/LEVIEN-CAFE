"use client";
import { useState } from "react";
export default function EventBookingForm(){const [sent,setSent]=useState("");const [error,setError]=useState("");const [saving,setSaving]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setSaving(true);setError("");const f=new FormData(e.currentTarget);try{const r=await fetch('/api/event-bookings',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(f.entries()))});const x=await r.json();if(!r.ok)throw new Error(x.error);setSent(x.referenceCode);e.currentTarget.reset()}catch(e){setError(e instanceof Error?e.message:'Unable to submit request.')}finally{setSaving(false)}}return <form className="eventBookingForm" onSubmit={submit}><div className="eventBookingGrid"><label>Event name *<input name="eventName" required maxLength={160} placeholder="Birthday celebration"/></label><label>Event type<input name="eventType" maxLength={80} placeholder="Birthday, meeting, private party…"/></label><label>Event date *<input name="eventDate" type="date" required/></label><label>Start time *<input name="startTime" type="time" required/></label><label>End time<input name="endTime" type="time"/></label><label>Expected guests<input name="guestCount" type="number" min="1" max="1000"/></label><label>Your name<input name="customerName" maxLength={120} placeholder="Name"/></label><label>Phone number<input name="customerPhone" type="tel" maxLength={30} placeholder="Phone"/></label><label>Email (optional)<input name="customerEmail" type="email" maxLength={254}/></label><label className="wide">Additional details<textarea name="notes" rows={5} maxLength={2000} placeholder="Tell us about your event…"/></label></div>{error&&<div className="contactFormError">{error}</div>}{sent&&<div className="contactFormSuccess"><span>✓</span><div><strong>Request received</strong><small>Reference {sent}. Our team will contact you soon.</small></div></div>}<button className="button primary contactSubmit" disabled={saving}>{saving?'Sending…':'Submit event request'}</button></form>}
